name: Deploy CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    
    - name: configure
      run: EMAIL=${{secrets.EMAIL}} DOMAIN=vca.ifba.edu.br bash ./deploy.sh
